export enum HttpStatusCode {
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  FORBIDDEN = 403,
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
  REQUEST_TIMEOUT = 408,
  SERVICE_UNAVAILABLE = 503,
  // Outros códigos de erro que você deseja mapear
}
