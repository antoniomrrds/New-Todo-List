import { StyledContainer } from '@/styles/global-styles';
import styled from 'styled-components';

export const StyledCenteredContainer = styled(StyledContainer)`
   align-items: center;
   display: flex;
  justify-content: center;
  flex-grow: 1; 
   margin: 0 auto;

`;
